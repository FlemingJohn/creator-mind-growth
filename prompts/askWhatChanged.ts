export function askWhatChanged(): string {
  return [
    "In two short sentences, what have you changed your mind about with this audience?",
    "",
    "Write it as if you are talking to the creator. No headings, no list."
  ].join("\n");
}
